/* 小程序中特定部分的统一样式 */

/* 各个分块的最高一级标题文本 */
.cm-module__title {
  font-weight: bold;
  font-size: 40rpx;
  color: black
}

/* 描述标题文本 */
.cm-desc__title {
  font-weight: bold;
  color: black;
}

/* 描述内容文本 */
.cm-desc__content {
  color: #cccccc;
}

/* 单行文本省略 */
/* 这里的省略不需要给定宽度，发现不够就会省略 */
.cm-sigle-line-ellipsis {
  display: -webkit-box;
  word-break: break-all;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 1;
  text-overflow: ellipsis;
  overflow: hidden;
}

/* 单行文本省略 */
/* 这里的省略需要给定宽度 */
.cm-sigle-line-ellipsis {
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
}